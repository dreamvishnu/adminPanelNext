import { PanelContent } from "@/components";

export default function Settings() {
  return (
    <PanelContent headerContent title="Settings">
      <h1>Settings Page</h1>
      <p>This is the settings page.</p>
    </PanelContent>
  );
} 