import { PanelContent } from "@/components";

export default function VenueProviders() {
  return (
    <PanelContent headerContent title="Venue Providers">
      <h1>Venue Providers Page</h1>
      <p>This is the venue providers page.</p>
    </PanelContent>
  );
} 